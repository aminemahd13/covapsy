# SLAM Toolbox parameters tuned for Pi 5 (2GB) with RPLIDAR A2M12
slam_toolbox:
  ros__parameters:
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Frame IDs
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan_filtered
    mode: mapping  # Switch to 'localization' after map is built

    # Pi 5 performance-tuned
    max_laser_range: 5.0
    minimum_travel_distance: 0.3
    minimum_travel_heading: 0.3
    map_update_interval: 0.3
    resolution: 0.05
    max_queue_size: 10
    stack_size_to_use: 40000000

    # Threading (Pi 5 has 4 cores, leave 2 for nav + drivers)
    solver_threads: 2

    use_scan_matching: true
    use_scan_barycenter: true
    transform_publish_period: 0.02
    tf_buffer_duration: 30.0
